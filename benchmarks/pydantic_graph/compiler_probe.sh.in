#!/bin/sh
set -eu

case "$(basename "$0")" in
  *cxx*)
    compiler=$ATOLL_REAL_CXX
    kind=cxx
    ;;
  *)
    compiler=$ATOLL_REAL_CC
    kind=cc
    ;;
esac

printf '%s\n' "$kind" >> "$ATOLL_COMPILER_PROBE_LOG"
exec "$compiler" "$@"
