"""Runtime helpers for Atoll-managed modules.

The runtime package verifies managed shim routing and runs target pytest gates in
controlled child processes. It does not build sidecars or change project source
files.
"""
